input {
  http {
    id => "0004_input_http.conf :: input//http_30000"
    host => "0.0.0.0"
    port => 30000

    add_field => { "data_type" => "http" }
    add_field => { "data_source" => "http" }

  }
}
